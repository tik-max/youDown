services:
  - type: web
    name: youtube-downloader-api

    env: node

    rootDir: backend

    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg
      npm install
      pip install yt-dlp

    startCommand: npm start
